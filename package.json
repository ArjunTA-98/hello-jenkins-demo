{
  "name": "hello-node",
  "version": "1.0.0",
  "scripts": { 
    "start": "node server.js" 
  },
  "dependencies": {
    // ADD THIS LINE: PM2 will now be installed locally during 'npm install'
    "pm2": "^5.3.1" 
  }
}
